$(document).ready(function()
        {
                // call the tablesorter plugin
                $("#distfiles").tablesorter( {sortList: [[0,0]] });
                $("#rsync").tablesorter( { sortList:[[0,0]]});
        }
);
